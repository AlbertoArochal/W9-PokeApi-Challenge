"use strict";
/*const pokeProfile = () => {
    const pokeLinks = document.querySelectorAll('.pokemon__profile');
    pokeLinks.forEach((pokelink) => {
        pokelink.addEventListener('click', () => {
            const pokeName = pokelink.textContent;
            const pokElement = document.createElement('div');
            document
                .querySelector('.Profile__container')
                ?.appendChild(pokElement);
        });
    });
};*/
